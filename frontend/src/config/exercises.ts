import type { Exercise } from "../types";

// Optional: add the exact IDs and names from your existing exercises table.
// No IDs are assumed: the backend currently has no exercise-catalog endpoint.
export const configuredExercises: Exercise[] = [];
