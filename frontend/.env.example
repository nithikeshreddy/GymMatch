# Public development defaults only. VITE_* values are visible to the browser.
# Never put passwords, API secrets, or database credentials in this file.
VITE_API_URL=/api
# Leave empty to connect through the development Socket.IO proxy.
VITE_SOCKET_URL=
# Used by the Vite development proxy only.
BACKEND_URL=http://localhost:3000
